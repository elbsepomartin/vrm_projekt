{% extends 'bookings/bookings.html.twig' %}

{% block content %}
    <h2>The list of bookings</h2>
    {%  if bookings is empty %}
        <p>You do not have any bookings.</p>
    {% else %}
        <table class="booking-table">
            <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Phone</th>
                <th>E-mail</th>
                <th>Birthday</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Arrival Time</th>
                <th>Number Of People</th>
                <th>Paying Method</th>
            </tr>
            </thead>
            <tbody>
            {% for booking in bookings %}
                <tr>
                    <td>{{ bookings.firstName }}</td>
                    <td>{{ bookings.lastName }}</td>
                    <td>{{ bookings.phone }}</td>
                    <td>{{ bookings.email }}</td>
                    <td>{{ bookings.birthday|date('d.m.Y') }}</td>
                    <td>{{ bookings.startDate|date('d.m.Y') }}</td>
                    <td>{{ bookings.endDate|date('d.m.Y') }}</td>
                    <td>{{ bookings.arrivalTime|date('H:i') }}</td>
                    <td>{{ bookings.nrOfPeople }}</td>
                    <td>{{ bookings.payingMethod }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" media="screen" href="/css/list.css">
{% endblock %}